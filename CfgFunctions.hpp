class CfgFunctions {
    class ADDON {
        tag = QUOTE(ADDON);
        class functions {
            file = "functions";
            // Reference README.md for usage instructions
            PATHTO_FNC(baseSpectator);
            PATHTO_FNC(briefing);
        };
    };
};
